# Meta Graph API — Instagram Publishing
# (mesmas credenciais usadas pela Sofia; só adicione permissão ig_content_publish ao token)
META_GRAPH_ACCESS_TOKEN=
IG_BUSINESS_ACCOUNT_ID=26416606091293081

# Cloudflare R2 (S3 compatível) — host das imagens
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=merge-creatives
R2_PUBLIC_BASE_URL=https://pub-xxxxxxxx.r2.dev  # ou R2_PUBLIC_BASE — qualquer um dos dois funciona

# Telegram alerts — bot dedicado @mergewellness_alerts_bot
# (canal isolado — NÃO compartilhar credenciais com Sofia/Oases)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Anthropic Claude — chave dedicada Merge (NUNCA reusar a da Sofia)
# Usada por: autogen (planner/writer/reviewer), news scorer, regen via Telegram
ANTHROPIC_API_KEY_MERGE=

# Discord webhook (mirror passivo, opcional) — replica notificações texto no canal
# Aprovações continuam só no Telegram. Deixe vazio pra desabilitar.
DISCORD_WEBHOOK_URL=
